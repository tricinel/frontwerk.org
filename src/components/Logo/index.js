import styled from 'styled-components';

const Logo = styled.img`
  display: inline-block;
  height: 40px;
  margin-right: 10px;
  max-width: 100%;
  padding-top: 10px;
  vertical-align: middle;
`;

export default Logo;
